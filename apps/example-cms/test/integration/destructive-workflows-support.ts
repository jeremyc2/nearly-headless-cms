export type { ExampleSystem } from "../../src/system.ts";
export {
  createTemporaryStorageRoot,
  firstItemIndex,
  httpNotFound,
  httpOk,
  isRecord,
  jsonRecord,
  removeStorageRoot,
  twoItems,
} from "./headless-api-support.ts";

const pageSize = 100;

export { pageSize };
