import { AnonymousIdentity } from "nearly-headless-cms/adapters";

/** Anonymous request identity for the example CMS. Replace this layer to attach real actors. */
export const {layer} = AnonymousIdentity;
