# Core

This folder is the CMS Builder surface for the minimal Example CMS.

Read these files in order:

1. `definitions.ts` — compile your Content Types into a Snapshot
2. `delivery.ts` — declare Headless Delivery Queries with `DeliveryRecipes`
3. `composition.ts` — wire `Filesystem.cms` and the HTTP transport

There is no presentation layer in this app.
